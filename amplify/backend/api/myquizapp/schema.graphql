type Score @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: AWSEmail
  score: Int
  time: AWSDateTime
}

type QuestionModel @model @auth(rules: [{allow: public}]) {
  id: ID!
  text: String
  a: String
  b: String
  c: String
  d: String
  e: String
  reversed: Boolean
  number: Int
}
 